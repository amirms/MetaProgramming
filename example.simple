b := 5 ;
c := (b-2);
z := 1;
while ( x > 0 ) do
{
	d := 1;
	while ( d < 100 ) do {
		d := (d+1);
	}
	z := (z*y);
	x := (x-1);
}
z := b;
a := z;
if ( a > z ) then {
	q := 4;
} else {
	q := 5;
}
if ( true ) then {
	doNotEliminate := true;
} else 
	deEliminate := true;

