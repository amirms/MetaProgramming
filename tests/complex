procedure thismethod( val rogier) {
	rogier := 4;
}

procedure othermethod( val rogier, test) {
	rogier := test;
}

procedure expmethod( val rogier, test) {
	rogier := test;
}

r := 1;
while ( not(o < g) ) {
	i := (i+1);
}
if ( e < r ) {
	derp := 0;
} else {
	blaat := r;
}
while ( 4 < 5 ) {
	while ( 5 < 4 ) {
		if ( 3 < 4 ) {
			somethingy := alot;
			alot := verymuch;
			call thismethod(val rogier);
		} else {
			call othermethod(val somethingy, blaat);
			call expmethod(val that, 4);
		}
	}
}